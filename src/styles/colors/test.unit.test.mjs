const { parseToRgb } = require('polished');
const createColor = require('./index');

// describe('default options', () => {
//   const color = createColor('blue');
//   const bits = parseToRgb(color);
//   console.log({ color, bits });

//   test('', () => {});
// });
