const services = {
  name: 'Services',
  path: '/services'
};

const styleguide = {
  name: 'Style Guide',
  path: '/styleguide'
};

const main = [services];

const navigation = {services, styleguide, main};

export default navigation;
