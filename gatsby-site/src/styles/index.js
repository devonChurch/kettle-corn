import createColor, {colors} from './colors';
import spacing from './spacing';
import dynamic from './dynamic';
import misc from './misc';
import media, {breakpoints} from './media';

const styles = { createColor, colors, spacing, misc, media, breakpoints, dynamic };

export {styles as default, createColor, colors, spacing, misc, media, breakpoints, dynamic };
